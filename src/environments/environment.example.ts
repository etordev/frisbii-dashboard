/**
 * Copy this file to environment.ts and set your Frisbii private API key
 * with the base URL in order to use the project.
 * environment.ts is gitignored so your key is never pushed.
 */
export const environment = {
  frisbiiApiKey: '',
  frisbiiApiBaseUrl: '',
} as const;
